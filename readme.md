PROYECTO DE HTML Y CSS

1. Realizar 1 Sitio web con html y css en base al gestor de ventas y productos del proyecto de SCRUM. (NO debe llevar codigo, solo el diseño)...

2. Aplicar todos los ejercicios que hemos hecho en clase al proyecto.

3. Todos los temas q hemos visto en clase de css y html son los siguientes:

Se deben usar:

-Variables

-Colores: esquemas hsl, rgb, lab

-Creacion de gradientes y parametros con direccionamiento de color (linear-gradient, radial-gradient, conic-gradient entre otros).

-Agregar imagenes, imagenes de fondo (videos si es posible): background-repeat,
          background-color,
          background-image,
          background-attachment,
          background-position, 
          background-clip, 
          background-origin, 
          background-size...

-Propiedad opacity, utilizando valores ::before y ::after

-Selectores de ETIQUETAS, ID, CLASE, MIXTOS, selectores de atributos: Basicos,
              Avanzados,
              Atributo existente,
              Atributo con valor exacto,
              Atributo con lista de palabras,
              Atributo con lista compuesta,
              Atributo con texto (contiene texto)
              Atributo comienzo ::after
              Atributo final ::before
              :Active and :disabled

-Metodologia BEM (Block, element, Modifier): Es 1 enfoque de nomenclatura de clases en CSS q ayuda a estructurar y organizar el codigo de manera mas clara y mantenible.              

-Combinadores de selectores: 
            Combinador descendiente
            COmbinador Hijo,
            Combinador hermano adyacente,
            COmbinador hermano general,
            Combinador Universal,
            Combinador hermano contiguo.

-combinadores logicos:
            selector :is
            selector :where
            selector :has
            selector :not

-Peseudoclases:
        De interaccion -> 
            (:hover, :active, :focus, :focus-within, :focus-visible).
        De ubicacion -> 
            (:any-link, :link, :visited, :target)
        De idioma -> (:lang, :dir)
        De estado -> (:fullscreen)
        De estructura -> (:root, :host)
        
        Funcionales -> 
            (:first-child, :last-child, :nth-child, :nth-last-child, :nth-child(A of B, :first-of-type, :last-of-type, :nth-of-type(n), :nth=last-of-type(n))

            Elementos unicos o sin hijos (:only-child, :only-of-type, :empty)

-Reglas como @media, @import:
            (@support, @media, @keyframes, @scope, @font-face, @font-features-values, @when/else, @page, @counter-style, @property, @layer.)

-Importaciones con condicioneles, importacion con medias queries.

-Tipografias, fuentes.

-Textos y alineaciones: 
            (text-align, text-align-last, text-justify, vertical-align, ).

-Expaciado de texto: 
            (letter-spacing, world-spacing, line-height, text-indent)

-Decoraciones de texto:
            (text-decoration, text-transform, text-underline)

-Representacion de datos-tablas:
            list-style-image,
            list-style-position,
            list-style-type,

-VIÑETAS PARA LAS LISTAS:
        listas con simbolos,
        listas numericas
        listas alfabeticas

-Bordes
        border-color
        border-width
        border-style

    Bordes-multiples
        border-top-width,
        border-top-style,
        border-top-color,
        border-right-width,
        border-right-style,
        border-right-color,
        border-bottom-width,
        border-bottom-style,
        border-left-witdh,
        border-left-style,
        border-left-color

        Esquinas individuales:
            border-top
            border-right
            border-bottom
            border-left


    Bordes-redondeados
        Border-radius

-Modelado de cajas: 
        border,
        margin,
        padding,
        content

        top,
        left,
        right,
        bottom,
        center

-Margenes y rellenos:
        margin-left
        margin-right
        margin-bottom
        margin-top

        padding-top
        padding-bottom
        padding-righ
        padding-left

-Unidades, unidades relativas.

-Propiedad display
        block,
        inline,
        inline-block,
        flex,
        inline-flex
        grid,
        inline-grid
        list-item
        table,
        table-cell,
        table-row,
        contents,
        none        

-Propiedad position
        static,
        relative
        absolute,
        fixed,
        sticky,
        top, bottom, right, left..

-Propiedad flex-box:
        static,
        relative,
        absolute,
        
        flex,
        inline,
        flex-wrap, (wrap, wrap-reverse, nowrap)
        flex-direction, (row, row-reverse, column, column-reverse).

        gaps: (row-gap, column-gap, )

- Alineacin con flex.
        justify-content:
            start,
            end,
            center,
            space-between,
            space-around,
            space-evenly
        align-items:
            start,
            end,
            center,
            stretch,
            baseline
        align-content:
            start,
            end,
            center,
            stretch,
            space-between
            space-around
        align-self:
            start,
            end,
            center,
            stretch,
            baseline.

        place-content

        order

-grid, responsive.
        grid-template-columns,
        grid-template-rows,
        grid-template,
        grid-template,areas
        grid-area

-medias-queries.

-Transiciones:
        transition-property,
        transition-duration,
        transition-timing-function,
        transition-delay
        transition-timing-function

        transicion de entrada, de salido, transiciones diferentes.

-Funciones de tiempo,funcion predefinida.

        



